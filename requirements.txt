# API and model serving. This is what the Docker image installs, so anything
# added here ships to production - keep analysis and frontend deps out.
# Pinned. Unpinned deps are the #1 cause of "it worked yesterday".
#
# Several versions below were bumped from the original plan's pins to find
# Python 3.14 wheels; see DECISIONS.md entry 2.
fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.52
pydantic==2.13.5
python-dotenv==1.0.1

# Postgres driver (used in production via DATABASE_URL)
psycopg[binary]==3.2.13

# Serving the forecast. pyarrow is not optional: predict.py reads the inference
# snapshot from parquet, and it used to arrive only as a streamlit dependency.
lightgbm==4.7.0
numpy==2.5.2
pandas==2.3.3
# 24.x, not 25.0.1: that release has a known segfault (apache/arrow#50471) and
# Streamlit Community Cloud force-downgrades it on sight. predict.py reads the
# inference snapshot through pyarrow on every cold start.
pyarrow==24.0.0

# --- Added in Stage 3B ---
# ortools==9.11.4210
# simpy==4.1.1

# --- Added in Stage 3C ---
# openai==1.59.0
