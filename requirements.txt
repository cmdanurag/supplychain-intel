# Pinned. Unpinned deps are the #1 cause of "it worked yesterday".
fastapi==0.141.1
uvicorn[standard]==0.34.0
sqlalchemy==2.0.52
pydantic==2.13.5
python-dotenv==1.0.1

# Postgres driver (used in production via DATABASE_URL)
psycopg[binary]==3.2.13

# Frontend
streamlit==1.62.0
altair==6.2.2  # streamlit's own pin (5.5.0) predates a Python 3.14 TypedDict fix
requests==2.32.3

# --- Added in Stage 3A ---
# Original plan pinned pandas 2.2.3 / numpy 2.2.1 / lightgbm 4.5.0 / scikit-learn 1.6.0,
# bumped for Python 3.14 wheel availability (same issue as fastapi/sqlalchemy/streamlit
# in Phase 0). numpy/pandas versions match what streamlit already pulled in.
pandas==2.3.3
numpy==2.5.2
lightgbm==4.7.0
scikit-learn==1.9.0
matplotlib==3.11.1

# --- Added in Stage 3B ---
# ortools==9.11.4210
# simpy==4.1.1

# --- Added in Stage 3C ---
# openai==1.59.0
